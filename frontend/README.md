# Frontend - TI Diagnostic Panel

```bash
npm install
copy .env.example .env
npm run dev
```

Variables:
- `VITE_API_URL` URL base del backend (por defecto `http://127.0.0.1:8000/api`).
- `VITE_API_KEY` clave API opcional si el backend tiene `API_KEY` activa.
